# Trellis 会话日志 — 2026-09

## 2026-09-07 — feat-09-07-inject-readonly-prompt（已归档）

- **任务**：规划期只读保护改为注入显式系统提示词（不再只裁剪工具列表）
- **工作类型**：feat
- **提交**：`2403dd8`（实现 + 任务产物 + spec 更新）
- **摘要**：
  - 只读语义升级为双通道：denylist 工具面裁剪之外，`system-prompt/assemble` 在 `undecided`/`planning`
    时向 sections 追加 `trellis:readonly` 指令段，让模型在提示词文本里显式读到"当前只读、方案获批前
    禁止改码"。
  - `lib/readonly.js` 新增 `READONLY_SECTION` 常量、`readonlyInstructionFor`（undecided/planning 中文
    文本，authorized/未知 → null）、`appendReadonlyInstruction`（去重同名段后尾部追加，非数组/authorized
    → null）；`lib/index.js` 处理器组合调用，authorized 时三函数全 null、装配结果与现状逐字节一致。
  - 采纳 design-review minor #2：planning 文案区分"源码/业务文件"与 `trellis_artifact_update` 规划
    产物通道，避免模型因字面禁令拒绝产出 prd/design；minor #1（英文面包屑 + 中文指令混排）按 PRD
    决策保留中文并记录为有意为之。
  - 测试：`test/readonly.test.js` 新增 8 组用例（23 项），全量 7 文件 91/91 通过（沙箱内
    `node --test` 子进程 spawn 受限，逐文件直接执行复核）。
  - 规范：`.trellis/spec/trellis-workflow/task-engine/index.md` 记录双通道只读语义、`trellis:readonly`
    预留 section 名，以及 `trellis_artifact_update` 默认 slug 跟随会话绑定任务的已知坑。

## 2026-09-06 — issue-09-06-refactor-scan-readonly（已归档）

- **任务**：重构模式 scan 阶段被视为执行中（只读授权阶段感知缺陷）
- **工作类型**：issue（跳过 analyze——根因在调研阶段已确认并写入 report/fix-note）
- **提交**：`163aaa9`（fix）→ `31b05f8`（mark completed）→ `d5a5a2a`（archive）
- **归档位置**：`.trellis/tasks/archive/2026-09/issue-09-06-refactor-scan-readonly`
- **摘要**：
  - 根因：相位/授权/展示只认 `task.status`、忽略 `work.stage`，写入端无 status↔stage 一致性校验；
    refactor 落在 `in_progress + scan` 时被当作执行中，只读授权放开。
  - 修复：`lib/state.js` 新增 `stagePhase`/`phaseForTask`（阶段感知相位，completed 恒胜）；`lib/index.js`
    全链路改用 `phaseForTask`；`lib/task.js` 写路径拒绝「规划型阶段 + in_progress」；`lib/board.js` 记录
    携带解析相位；`lib/client.js` 看板/状态标签按解析相位展示。
  - 测试：新增 `test/state.test.js`；`test/index.test.js` 补 create/update 拒绝用例与 board phase 断言。
  - 规范：`.trellis/spec/trellis-workflow/task-engine/index.md` 重述只读模型并新增已知坑「status 与
    work.stage 必须强耦合」。
- **验证**：单进程断言 46 项通过（state/readonly/task/board 全链路）；`node --check` 全过；沙箱内
  `node --test` 子进程 spawn 受限，完整套件待正常环境 `pnpm test` 复核。

## 2026-09-06 — issue-09-06-prune-system-prompt-tools（已归档）

- **任务**：修复规划期只读保护下 System Prompt 工具文本未同步修剪问题
- **工作类型**：issue
- **提交**：`1ebe077`（fix & tests）→ `c6cb639`（mark completed）→ `285151d`（archive）
- **归档位置**：`.trellis/tasks/archive/2026-09/issue-09-06-prune-system-prompt-tools`
- **摘要**：
  - 现象：Web 端开启 `enforceReadonlyPlanning` 后，UI 调试栏中工具列表虽已过滤，但发送给模型的自然语言 System Prompt 中仍残留 `write`、`edit` 等写工具指南，诱发模型越权调用；另外 `lib/client.js` 中 `closeExpanded` 的 `useCallback` 位于条件 return 之后导致 Hooks 顺序异常。
  - 根因：`system-prompt/assemble` 钩子仅裁剪了 `assembled.tools`，未对各个插件通过 `ctx.systemPrompt.section` 注册的 `tool:<name>` section（如 `tool:write`、`tool:edit`）进行过滤，导致系统提示词文本拼接未同步受控。
  - 修复：
    1. `lib/client.js`：将 `closeExpanded` 移至条件 return 之前，修复 React Hooks 规则违反。
    2. `lib/readonly.js`：导出 `applyReadonlySections`，精准过滤非白名单的 `tool:*` sections。
    3. `lib/index.js`：在 `system-prompt/assemble` 钩子中同步修剪 `sections` 并合并返回。
  - 验证：新增 4 组 `applyReadonlySections` 单测，15 项只读测试与 39 项核心套件全部 100% 通过；已打包为 `.tgz` 并在宿主环境中复核验证成功。

## 2026-09-06 — refactor-09-06-kanban-ui-redesign（已归档）

- **任务**：重构任务看板UI（列表与看板解耦及图标化）
- **工作类型**：refactor
- **提交**：`cdb0616`（图标化+列表解耦）→ `c5ead02`（空泳道折叠+自动选中）→ `3106f00`（纯图标按钮+详情检查器）→ `7c92e1f`（gitignore tgz）
- **归档位置**：`.trellis/tasks/archive/2026-09/refactor-09-06-kanban-ui-redesign`
- **摘要**：
  - 小看板彻底解耦为高密度垂直任务列表（Master-Detail），消灭 400px 内双列看板导致的标题截断；大看板按工作流泳道专注展示并智能折叠 0 任务泳道，消除三条原生横向滚动条并发问题。
  - 自包含内联 SVG 图标体系（refresh/maximize/close/search/file/folder/check/clock/alert 等）全面替换 Emoji 与汉字按钮，展开/收起彻底纯图标化（保留 title 可访问性）。
  - 详情面板升级：横向 Step Tracker 流水线指示器、IDE 风格文件树产物行、胶囊化属性标签（类型/状态/阶段）、实心品牌色主按钮「推进任务」。
  - 空列状态不再打印 `(空)` 汉字，改为微透留白插槽；弹窗锁定舒适比例并注入细滚动条样式。
  - 产物：`banana-peeljj12-dsh-trellis-0.3.0-rc.5.tgz`（564.9 kB）。
- **验证**：`node --check lib/client.js` 语法全过；交互验收由用户在 Web 界面确认；沙箱内 `node --test` 子进程 spawn 受限，完整套件待正常环境 `pnpm test` 复核。

